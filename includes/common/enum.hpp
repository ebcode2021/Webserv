#ifndef ENUM_HPP
# define ENUM_HPP

enum	blockType
{
	server,
	location,
	other,
};

enum	status
{
	off,
	on,
};

enum	fileMode
{
	fail,
	success,
	test,
};

enum	multiplicityOption
{
	single,
	multiple,
};
#endif